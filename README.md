# If you want run this code simple dobule click on the run_dev.bat file
# This will run the code in the dev environment

# If you want to run code in the shell, you can use the following command:
# bash run_dev.sh
# This will run the code in the dev environment